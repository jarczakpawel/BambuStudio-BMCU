name: Build portable

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    tags: [ "v*" ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            name: linux
          - os: windows-latest
            name: windows
          - os: macos-15
            name: mac_universal

    runs-on: ${{ matrix.os }}
    timeout-minutes: 360

    env:
      CACHE_REV: v1

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true
          submodules: recursive
          fetch-depth: 0

      - name: Compute deps cache key (non-windows)
        if: matrix.name != 'windows'
        shell: bash
        run: |
          set -euo pipefail
          deps_tree="$(git rev-parse HEAD:deps 2>/dev/null || echo no-deps)"
          echo "DEPS_TREE=$deps_tree" >> "$GITHUB_ENV"
          echo "DEPS_TREE=$deps_tree"

      - name: Compute deps cache key (windows)
        if: matrix.name == 'windows'
        shell: pwsh
        run: |
          $depsTree = (git rev-parse HEAD:deps)
          Add-Content $env:GITHUB_ENV "DEPS_TREE=$depsTree"
          Write-Host "DEPS_TREE=$depsTree"

      # ---------------- Linux ----------------
      - name: Linux - install system deps
        if: matrix.name == 'linux'
        shell: bash
        run: |
          sudo ./BuildLinux.sh -ur
          sudo chown $USER -R ./

      - name: Linux - cache deps
        if: matrix.name == 'linux'
        id: cache_linux_deps
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/deps/build/destdir
          key: ${{ runner.os }}-${{ matrix.name }}-deps-${{ env.DEPS_TREE }}-${{ env.CACHE_REV }}

      - name: Linux - build deps if missing
        if: matrix.name == 'linux' && steps.cache_linux_deps.outputs.cache-hit != 'true'
        shell: bash
        run: |
          ./BuildLinux.sh -dfr

      - name: Linux - build + install (portable dir)
        if: matrix.name == 'linux'
        shell: bash
        run: |
          set -euo pipefail
          rm -rf build install-dir out
          mkdir -p out

          cmake -S . -B build -G Ninja \
            -DCMAKE_PREFIX_PATH="${PWD}/deps/build/destdir/usr/local" \
            -DSLIC3R_STATIC=1 \
            -DSLIC3R_GTK=3 \
            -DBBL_RELEASE_TO_PUBLIC=1 \
            -DBBL_INTERNAL_TESTING=0 \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="${PWD}/install-dir"

          cmake --build build --target install -j"$(nproc)"

          test -x install-dir/bin/bambu-studio || (echo "ERROR: missing install-dir/bin/bambu-studio" && exit 1)

          tar -C install-dir -czf "out/BambuStudio_Linux_portable_${GITHUB_SHA::7}.tar.gz" .
          (cd out && sha256sum * > SHA256SUMS.txt)

      # ---------------- Windows ----------------
      - name: Windows - setup MSVC
        if: matrix.name == 'windows'
        uses: microsoft/setup-msbuild@v2

      - name: Windows - install tooling
        if: matrix.name == 'windows'
        shell: pwsh
        run: |
          choco install -y strawberryperl pkgconfiglite

      - name: Windows - cache deps
        if: matrix.name == 'windows'
        id: cache_win_deps
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}\deps\build\BambuStudio_dep
          key: ${{ runner.os }}-${{ matrix.name }}-deps-${{ env.DEPS_TREE }}-${{ env.CACHE_REV }}

      - name: Windows - build deps if missing
        if: matrix.name == 'windows' && steps.cache_win_deps.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force "${{ github.workspace }}\deps\build" | Out-Null
          Set-Location "${{ github.workspace }}\deps\build"
          cmake .. -G "Visual Studio 17 2022" -A x64 -DDESTDIR="./BambuStudio_dep" -DCMAKE_BUILD_TYPE=Release -DDEP_DEBUG=OFF
          msbuild /m ALL_BUILD.vcxproj /p:Configuration=Release

      - name: Windows - build + install + pack zip
        if: matrix.name == 'windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force "${{ github.workspace }}\build" | Out-Null
          New-Item -ItemType Directory -Force "${{ github.workspace }}\install-dir" | Out-Null
          New-Item -ItemType Directory -Force "${{ github.workspace }}\out" | Out-Null

          Set-Location "${{ github.workspace }}\build"
          cmake .. -G "Visual Studio 17 2022" -A X64 `
            -DBBL_RELEASE_TO_PUBLIC=1 -DBBL_INTERNAL_TESTING=0 `
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}\deps\build\BambuStudio_dep\usr\local" `
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}\install-dir" `
            -DCMAKE_BUILD_TYPE=Release
          cmake --build . --target install --config Release -- -m

          $exe1 = "${{ github.workspace }}\install-dir\bin\bambu-studio.exe"
          $exe2 = "${{ github.workspace }}\install-dir\bambu-studio.exe"
          if (!(Test-Path $exe1) -and !(Test-Path $exe2)) { throw "Missing bambu-studio exe in install-dir" }

          $zip = "${{ github.workspace }}\out\BambuStudio_Windows_portable_${{ github.sha }}.zip"
          if (Test-Path $zip) { Remove-Item -Force $zip }
          Compress-Archive -Path "${{ github.workspace }}\install-dir\*" -DestinationPath $zip -Force

          $sum = (Get-ChildItem -Recurse "${{ github.workspace }}\install-dir" -File | Measure-Object -Sum Length).Sum
          Write-Host ("Install-dir bytes: " + $sum)

          $h = Get-FileHash -Algorithm SHA256 $zip
          "$($h.Hash)  $([System.IO.Path]::GetFileName($zip))" | Out-File -Encoding ascii "${{ github.workspace }}\out\SHA256SUMS.txt"

      # ---------------- macOS ----------------
      - name: macOS - cache deps
        if: matrix.name == 'mac_universal'
        id: cache_mac_deps
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/deps/build
          key: ${{ runner.os }}-${{ matrix.name }}-deps-${{ env.DEPS_TREE }}-${{ env.CACHE_REV }}

      - name: macOS - install brew deps
        if: matrix.name == 'mac_universal'
        shell: bash
        run: |
          brew install automake texinfo nasm yasm x264 ninja

      - name: macOS - pin CMake 3.31.0 (avoid CMake 4.x)
        if: matrix.name == 'mac_universal'
        shell: bash
        run: |
          set -euo pipefail
          brew unlink cmake || true
          brew tap-new "$USER/old-cmake"
          brew extract --version=3.31.0 cmake "$USER/old-cmake"
          brew install "$USER/old-cmake/cmake@3.31.0"
          brew link --overwrite cmake@3.31.0
          brew pin cmake@3.31.0
          cmake --version

      - name: macOS - build deps if missing (arm64/x86_64)
        if: matrix.name == 'mac_universal'
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -d "deps/build/x86_64/BambuStudio_deps/usr/local" ] || [ ! -d "deps/build/arm64/BambuStudio_deps/usr/local" ]; then
            ./BuildMac.sh -d -x -a universal -t 10.15 -1
          fi

      - name: macOS - build slicer (universal)
        if: matrix.name == 'mac_universal'
        shell: bash
        run: |
          ./BuildMac.sh -s -x -a universal -t 10.15 -1
          mkdir -p out
          test -d "build/universal/BambuStudio/BambuStudio.app" || (echo "ERROR: missing .app" && exit 1)
          ditto -c -k --sequesterRsrc --keepParent "build/universal/BambuStudio/BambuStudio.app" "out/BambuStudio_Mac_universal_${GITHUB_SHA::7}.zip"
          (cd out && shasum -a 256 * > SHA256SUMS.txt)

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: bambustudio-${{ matrix.name }}-${{ github.sha }}
          path: out/
          if-no-files-found: error
